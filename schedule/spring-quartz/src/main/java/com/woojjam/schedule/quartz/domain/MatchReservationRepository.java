package com.woojjam.schedule.quartz.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MatchReservationRepository extends JpaRepository<MatchReservation, Long> {
}

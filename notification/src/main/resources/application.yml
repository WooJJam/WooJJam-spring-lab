spring:
  application:
    name: notification
  profiles:
    group:
      local: domain
  jpa:
    hibernate:
      ddl-auto: create
logging:
  level:
    co.kr.woojjam.notification: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

spring:
  application:
    name: aop
  profiles:
    group:
      local: domain
      dev: domain
#  jpa:
#    hibernate:
#      ddl-auto: create
logging:
  level:
    co.kr.woojjam.aop: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
---
spring:
  config:
    activate:
      on-profile: local
---
spring:
  config:
    activate:
      on-profile: dev
